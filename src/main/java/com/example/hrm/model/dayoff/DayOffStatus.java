package com.example.hrm.model.dayoff;

public enum DayOffStatus {
    
    WAITING,
    APPROVED,
    REJECTED;

    public static final String DESCRIPTION = "0 -> Waiting, 1 -> Approved, 2 -> Rejected";
}
